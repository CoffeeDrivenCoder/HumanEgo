# ==============================================================================
# Project Aria DINO-SAM2 Configuration
# ==============================================================================

dino_model_id: "IDEA-Research/grounding-dino-tiny"
dino_local_path: ""        # Optional: local HuggingFace snapshot dir; env HUMANEGO_DINO_MODEL_PATH overrides it.
hf_local_files_only: false # Optional: true for offline/cache-only loading; env HUMANEGO_HF_LOCAL_ONLY overrides it.
box_threshold: 0.3
sam2_repo_id: "facebook/sam2-hiera-tiny"
sam2_checkpoint_name: "sam2_hiera_tiny.pt"
sam2_checkpoint_path: ""   # Optional: local .pt path; env HUMANEGO_SAM2_CHECKPOINT overrides it.
sam2_config: "configs/sam2/sam2_hiera_t.yaml"

# --- Visual Prompts ---
dinosam_prompt:
  obj1: "a pretty cat ."
  arm: "human arms . human hands ."
